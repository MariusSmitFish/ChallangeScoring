# Required. Supabase → Project Settings → API
# URL must be the project base only, e.g. https://abcd.supabase.co
# (do NOT add /rest/v1 — the client adds that and you would get 404s)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
